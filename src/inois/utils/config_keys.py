class ConfigKeys:
    WORKING_DIRECTORY = "working_directory"
    FILES = "files"
    COLUMNS_TO_HASH = "columns_to_hash"
    USERNAME = "username"

    VALID_KEYS = (WORKING_DIRECTORY, FILES, COLUMNS_TO_HASH, USERNAME)
